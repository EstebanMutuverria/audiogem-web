# OpenSpec — Changes Archive

This directory serves as the audit trail for all completed and archived SDD changes.

Archived changes are moved to a folder named `YYYY-MM-DD-{change-name}/` within this directory. Do not modify or delete archived changes.
